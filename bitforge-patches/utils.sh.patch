diff --git a/utils.sh b/utils.sh
index 18996a1..e72b980 100755
--- a/utils.sh
+++ b/utils.sh
@@ -1,11 +1,11 @@
 #!/usr/bin/env bash
 
-APP_NAME="${APP_NAME:-VSCodium}"
+APP_NAME="${APP_NAME:-BitForge}"
 APP_NAME_LC="$( echo "${APP_NAME}" | awk '{print tolower($0)}' )"
-ASSETS_REPOSITORY="${ASSETS_REPOSITORY:-VSCodium/vscodium}"
-BINARY_NAME="${BINARY_NAME:-codium}"
-GH_REPO_PATH="${GH_REPO_PATH:-VSCodium/vscodium}"
-ORG_NAME="${ORG_NAME:-VSCodium}"
+ASSETS_REPOSITORY="${ASSETS_REPOSITORY:-SoC-One/BitForge}"
+BINARY_NAME="${BINARY_NAME:-bitforge}"
+GH_REPO_PATH="${GH_REPO_PATH:-SoC-One/BitForge}"
+ORG_NAME="${ORG_NAME:-BitForge}"
 
 # All common functions can be added to this file
 
